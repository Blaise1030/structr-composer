{
  "name": "div",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx tsc src/package/** --outDir package/src --declaration true | cp README.md ./package",
    "publish": "cd package && npm publish",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "structr-composer": "^1.0.9",
    "tailwind-merge": "^2.4.0"
  }
}
